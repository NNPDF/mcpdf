[tool.poetry]
name = "mcpdf"
version = "0.1.0"
description = ""
authors = ["Alessandro Candido <candido.ale@gmail.com>"]
packages = [
  { include = "mcpdf", from = "src" },
  { include = "frompdf", from = "src" },
  { include = "frompseudodis", from = "src" },
  { include = "fromdis", from = "src" },
]

[tool.poetry.dependencies]
python = "^3.9"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
